# Backend API base URL
# Leave empty when the frontend is served by the backend (single-service deploy)
# Set to your Render backend URL when the frontend is hosted separately (e.g. Vercel):
VITE_API_URL=https://aura-studio-c49i.onrender.com
